{
  "name": "noise",
  "description": "Make sound response when events happen",
  "private": true,
  "devDependencies": {
    "grunt": "^1.0.4",
    "grunt-contrib-sass": "^1.0.0",
    "grunt-contrib-watch": "^1.1.0",
    "load-grunt-tasks": "^3.4.0"
  },
  "scripts": {
    "start": "grunt watch",
    "build": "web-ext build --artifacts-dir=zips --ignore-files *.md Gruntfile.js *.zsh **/*.scss package*.json"
  },
  "license": "MIT"
}
